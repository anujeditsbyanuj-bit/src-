# --- Telegram & Bot Frameworks ---
kurigram
TgCrypto
pycryptodome  # AES/DES3 decryption for Akbots/meow_crypto.py (MeowTV/MeowVerse providers)

# --- Asynchronous & Networking ---
aiohttp==3.9.5
requests
brotli               # Akbots/goon_provider.py -> decodes br-encoded responses when resolving m3u8 links
GitPython           # Akbots/github_tools.py -> /downloadrepo (git clone)
PyGithub==2.1.1     # Akbots/github_tools.py -> /fork, /forkall, collaborators; also Akbots/repo_upload.py -> /release
m3u8                # M3U8 playlist parsing for Akbots/m3u8dl.py
heroku3             # Akbots/heroku_tools.py -> Heroku app management
telegraph           # Akbots/misc_tools.py -> /tgm, /telegraph
pymongo             # Akbots/misc_tools.py -> /mongochk (connection test)

# --- Database & Utilities ---
motor

# --- Web Framework (Keep Alive) ---
Flask==2.3.3
flask-cors          # Akbots/meow_proxy.py -> local HLS proxy fallback (MEOW_LOCAL_PROXY=true)
gunicorn==21.2.0
Jinja2==3.1.2
aiofiles           # async template reads for Akbots/filetolink (stream/download watch pages)
werkzeug==2.3.7
itsdangerous==2.1.2

# --- Hotstar API (in-process, see Akbots/hotstar_local_server.py) ---
# services/hotstar-api/main.py is a FastAPI app started automatically inside
# the bot's own event loop at startup (bot.py) — no separate deploy/env var
# needed. If you'd rather run it as its own standalone service instead
# (e.g. to offload it to another host), see services/hotstar-api/README.md;
# these three packages stay required either way since hotstar_local_server.py
# imports main.py's `app` object directly.
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9

# --- Media Downloader (/yt, /yta) ---
# Pinned to a recent release — YouTube/Instagram break old yt-dlp versions
# often, and an unpinned "yt-dlp" can silently stay on a stale cached
# install across redeploys on Render/Railway. Bump this periodically, or
# run `pip install -U yt-dlp` on the host directly if downloads start
# failing again.
# [default] extra bundles yt-dlp-ejs — the external-JS-challenge-solver
# component yt-dlp has required for full YouTube support since 2025.11.12.
# Without it, extraction can still succeed but the actual googlevideo
# download 403s (signature/n-param never gets solved) — see Akbots/ytdl.py's
# js_runtimes opt for the runtime side of this same requirement.
yt-dlp[default]>=2026.07.04
# Optional YouTube IP-block-avoidance fallback — see config.py's
# BROKENX_API_KEY comment and Akbots/brokenx_fallback.py.
brokenxapi
# NOTE: Akbots/ytdl.py also falls back to a vendored copy of youtube-dl
# (repo root: ytdl_legacy/) as a last-resort extractor tier for links every
# yt-dlp attempt fails on. It's pure stdlib — no pip package needed, nothing
# to add here — just don't delete the ytdl_legacy/ folder from the repo.

# --- YouTube PO Token auto-generation (fixes "Sign in to confirm you're
# not a bot" / 403 errors without needing manual cookies) ---
# Registers itself as a yt-dlp plugin automatically on install; no extra
# code wiring needed. Falls back silently to cookies if unavailable.
bgutil-ytdlp-pot-provider>=1.2.0

# --- Web scraping (Akbots/mediafire.py precise HTML parsing) ---
beautifulsoup4
curl_cffi>=0.10,<0.14  # NOT 0.6.x/0.7.x/0.8.x/0.9.x — yt-dlp's own impersonate feature explicitly rejects those as "unavailable" even though pip installs them fine (see yt-dlp's curl-cffi dependency spec)

# --- Fembed-family embed-host mirrors (Akbots/fembed.py) ---
lk21==1.6.0

# --- Gallery Downloader (/gallery) ---
gallery-dl

# --- Headless-browser video discovery fallback (JS-rendered players yt-dlp
# can't parse from raw HTML) — used by Akbots/headless.py ---
# `pip install playwright` alone does NOT ship the browser binary — it
# needs a separate `playwright install chromium` step. That's now handled
# automatically:
#   - Docker deploys: the Dockerfile runs it at build time.
#   - Non-Docker (Procfile/buildpack) deploys: Akbots/headless.py
#     self-installs it on first use (one-time ~30-90s delay, cached after).
# Either way, no manual step needed. If it can't install for some reason,
# this fallback just no-ops and the bot falls through to gallery-dl /
# raw-file download as before.
# Pinned to 1.54.0 — camoufox (below) needs this exact version; newer
# Node drivers (1.55+) crash on `pageError.location.url` for real
# Cloudflare-protected pages, which is what camoufox is used against.
# Also used by Akbots/headless.py's generic JS-rendered-player fallback
# and by Akbots/hotstar_browser.py's Hotstar-specific MPD+license URL
# capture (the /api/resolve_browser fallback in
# services/hotstar-api/main.py, for when the normal widget-API resolve
# fails outright) — same package, no extra install needed for either.
playwright==1.54.0

# --- Speed Test (/speedtest) ---
speedtest-cli

# --- AutoPost daily scheduler (/autotest, TMDB movie posts) ---
apscheduler

# --- System binaries needed for some downloaders (install via apt, NOT pip) ---
# ffmpeg      -> already required for /fb thumbnails, video metadata
# aria2       -> needed for magnet/.torrent links (/torrent), FTP links
#                (/ftp, Akbots/aria2_dl.py), and resumable direct-HTTP
#                downloads (Akbots/urluploader.py)
# megatools   -> needed for Mega.nz links (/mega)
feedparser

# --- Google Drive folder/private-file support (/gdrive) — OAuth API ---
# Needs an admin-generated token.pickle (see GDRIVE_SETUP.md /
# gdrive_oauth_setup.py) uploaded via /setgdrivetoken. Without it, /gdrive
# just falls back to the existing public-file-only method — nothing breaks.
google-api-python-client
google-auth
google-auth-oauthlib
natsort

# --- Archive tools (/zip, /unzip — Akbots/archive.py) ---
# pyzipper: password-protected zip creation for /zip (AES-256, AES-128, and
# legacy ZipCrypto via /zipencryption). Falls back to a plain (unencrypted)
# zip if this isn't installed — nothing breaks.
# py7zr: pure-Python 7z archive creation for /zipformat 7z — no external
# `7z`/`p7zip` binary needed to build .7z files (unlike /unzip's extraction
# path below, or RAR creation, both of which do need a system binary).
# Falls back to reporting the format unavailable if not installed.
# Extraction (/unzip) uses the system `7z` binary instead (see Dockerfile),
# not a pip package, since it needs to handle rar/7z/tar/gz/etc too.
pyzipper>=0.3.6
py7zr>=0.21.0

# --- JioSaavn music downloader (/saavn — Akbots/jiosaavn.py) ---
# mutagen: writes title/album/artist/cover-art tags onto the downloaded
# .m4a before it's sent.
mutagen

# --- JDownloader integration (/jd — covers hundreds of file hosts yt-dlp
# doesn't: premium hosters, click'n'load containers, DLC files, etc.) ---
# httpx: async HTTP client the myjd/ package (MyJDownloader API client)
# needs to talk to the JDownloader instance. The actual JDownloader.jar +
# Java are installed separately (see Dockerfile / JDOWNLOADER_SETUP.md) —
# this repo only needs httpx for the API side.
#
# NOTE: version range widened (was two conflicting pins: an unpinned
# "httpx" here AND a duplicate "httpx==0.25.2" further down in the
# musicbot section). That duplicate line caused pip's resolver to try
# satisfying two different constraints on the same package at once, which
# is what triggered the ResolutionImpossible loop against google-genai's
# pydantic>=2.0.0 requirement. Keep httpx declared ONCE, here, for the
# whole project. Range matches what google-genai itself requires
# (httpx>=0.28.1,<1.0.0) so pip doesn't have to backtrack.
httpx>=0.28.1,<1.0.0

# h2: HTTP/2 support for httpx.AsyncClient(http2=True) — used by
# terabridge_account.py's per-account sessions and terabox.py's shared
# retry client (TeraBridge feature-parity layer: non-blocking client with
# HTTP/2 multiplexing + retry on 5xx).
h2

# --- Headless-browser shortlink bypass (Akbots/playwright_bypass.py) ---
# Fallback bypass tier for shortlink sites the fast HTTP-only dispatcher
# (shortener_lib/) doesn't cover — runs the bundled bypass-shortlinks
# userscript in real Chromium. Reuses the SAME `playwright==1.54.0` pip
# package and Chromium install already set up above for Akbots/headless.py
# (see that block's comments — Dockerfile installs it at build time;
# non-Docker hosts self-install on first use, shared across both modules).
# Nothing extra to add here. If Chromium genuinely isn't available on a
# given host, this tier just fails soft — /bypass and group auto-detect
# keep working via the fast tier's own domain coverage regardless.

# upstash-redis: optional, used only by Akbots/terabridge_account.py for
# persisted multi-account TeraBox rotation (see config.py's
# UPSTASH_REDIS_REST_URL/TOKEN). If those env vars aren't set, that
# module falls back to in-memory single-account mode automatically —
# this package just needs to be importable.
upstash-redis

# --- Cloudflare bypass (Akbots/cf_bypass.py, Akbots/cf_lib/) — two
# in-process paths for scraping sites behind Cloudflare's "Just a
# moment..." / Turnstile challenge, tried in order:
#   1. DrissionPage — a real Chromium browser driven via CDP. Lightweight,
#      no extra service.
#   2. camoufox (below) — a stealth Firefox build, ported in-process from
#      the standalone TurnstileSolver/ project's app/solver.py (now at
#      Akbots/cf_lib/turnstile_solver.py). Stealthier against sites that
#      fingerprint DrissionPage/WebDriver, used as the fallback when #1
#      fails. Needs a running Xvfb (see entrypoint.sh) since real
#      Cloudflare deployments refuse to mount the Turnstile widget for a
#      headless browser.
DrissionPage==4.0.5.6
camoufox[geoip]>=0.4

# --- Shortlink / mirror-host bypass (LinkBypassTG/ and Link-Bypass/ —
# bundled sub-projects for bypassing URL shorteners like exe.io/sub2unlock
# and mirror hosts like GDToT/HubDrive/DriveFire/KatDrive) ---
# cfscrape is deprecated upstream in favor of cloudscraper, but both repos
# still import it directly, so both are kept. NOTE: their own
# requirements.txt also pins urllib3==1.26 — deliberately NOT applied here,
# since it would downgrade the newer urllib3 this project's other
# dependencies (requests/yt-dlp/etc.) expect; pin it yourself in a venv if
# you run either of these two as their own standalone service instead.
cloudscraper
cfscrape
lxml
python-dotenv

# --- /hdhub command + auto-post (Akbots/hdhub.py, Akbots/hdhub_lib/) —
# scraping/caching/storage code ported in from a standalone HdhubScraper
# project as proper submodules of this bot; aiohttp, beautifulsoup4, and
# apscheduler above already satisfy everything it needs. That source
# project's own bot.py (python-telegram-bot based — a second, unrelated
# Telegram framework) and requirements.txt were NOT kept; nothing here
# runs them, only the framework-independent scraper/cache/database logic
# was worth bringing in.

# --- /dub + /dubpdf commands (Akbots/dub.py) — ported in-process from the
# standalone VideoDubbing/ project's pipeline: translate (deep-translator)
# + re-voice (edge-tts) + ffmpeg mux for video, OCR (pymupdf/pdf2image/
# pytesseract) + rebuild for PDFs. VideoDubbing/'s own bot.py, config.py,
# and separate Client/bot token weren't kept — nothing here runs a second
# bot process any more, only its actual pipeline logic, wired into this
# bot's own commands. One deliberate swap vs. the original: transcribes
# with faster-whisper instead of openai-whisper — CTranslate2, not
# PyTorch, so tens of MB instead of multiple GB, and no GPU needed for the
# small/base models this uses on CPU.
ffmpeg-python==0.2.0
edge-tts
deep-translator==1.11.4
indic-transliteration==2.3.82
pymupdf==1.24.1

# --- /summarize command (Akbots/summarizer.py) — PDF export of the
# generated synthesis, using the Mukta.ttf font already bundled in
# Akbots/fonts/ for Hindi/Devanagari support.
fpdf2>=2.7.6
python-docx>=1.1.0   # /aiulta document intelligence — reads .docx replies
pdf2image==1.17.0
pytesseract==0.3.10
faster-whisper

# --- Voice cloning for /dub (male1/male2/female — see Akbots/voices/) ---
# Coqui TTS's XTTS-v2 model, used for zero-shot voice cloning from the
# bundled reference clips. Unlike everything else /dub uses, this DOES
# need PyTorch (there's no good lightweight alternative for real cloning
# today) — pull it in only if you actually want cloned-voice dubbing;
# /dub still works with a stock edge-tts voice if this isn't installed.
#
# DISABLED: Coqui TTS has no PyPI release compatible with Python 3.12
# (the version Render's Docker image builds with) — pinning it here
# crashed the entire deploy. Uncomment only if you switch to a Python
# <=3.11 base image AND actually need voice cloning.
# TTS

# NOT added, on purpose:
#   pyrogram==2.0.106   -> conflicts with this project's `kurigram` (a
#                          Pyrogram fork that installs under the SAME
#                          `pyrogram` import name); installing both would
#                          make pip fight over which one's files "win".
#   openai/whisper (git) -> multi-GB install (pulls in PyTorch), plus
#                          needs a system ffmpeg binary; faster-whisper
#                          (above) covers /dub's transcription instead —
#                          not needed at all now that VideoDubbing/'s own
#                          bot.py isn't kept as a separate process.

# --- musicbot/ (vendored RAXMUSIC group-voice-chat music bot + its
#     admin/fun plugins) — everything below is new for that module.
#     NOT added, on purpose: pyrogram==2.0.106 (conflicts with kurigram,
#     see note above — kurigram already provides the same `pyrogram`
#     import); asyncio==3.4.3 (legacy Py2-era backport, breaks Python
#     3's builtin asyncio module if installed); motor/heroku3/telegraph/
#     tgcrypto/requests/python-dotenv/GitPython (already listed above).
#     httpx (was pinned here as ==0.25.2, a second conflicting pin —
#     removed; the single httpx range declared above covers this too).
#     Also removed as duplicates (same reasoning as the httpx case above —
#     redundant declarations just give pip's resolver two constraints to
#     reconcile on the same package): speedtest-cli (already declared in
#     the Speed Test section), ffmpeg-python (was unpinned here, conflicting
#     with the ==0.2.0 pin in the /dub section — that pin wins since
#     faster-whisper's pipeline there needs a known-good version),
#     apscheduler (already declared in the AutoPost scheduler section),
#     aiofiles (already declared in the Web Framework section), and
#     beautifulsoup4 (already declared in the Web scraping section).
py-tgcalls>=2.2.0
pillow>=10.0.0
spotipy
youtube-search
youtube-search-python
pykeyboard
pyshorteners
pyfiglet
qrcode
emojis
unidecode
gtts
pydub
psutil
wget
pytz
future==0.18.3
dnspython
python-whois
hachoir
Faker
pycountry
search_engine_parser
gpytranslate
lexica-api==1.5.1
openai>=1.40.0,<2.0.0
deepai
bard
# pornhub-api==0.2.0
# REMOVED — this package (all releases up to and including the latest
# 0.4.0) hard-pins pydantic<2.0, while google-genai/openai/fastapi in
# this same file all require pydantic>=2.0.0. These two constraints can
# never be satisfied together, which is what was causing pip to loop
# through every google-genai release looking for a match. The upstream
# project has an open (unmerged) PR to bump to pydantic 2.x, so this is
# a real upstream incompatibility, not a version-pin mistake on our
# side. If you actually need this package, it must run in its own
# separate venv, isolated from google-genai/openai/fastapi.
git+https://github.com/AsmSafone/SafoneAPI

# --- /gemini, /ai, /ask commands (Akbots/gemini_chat.py) — official
# Google GenAI SDK, talks to the real Gemini public API with a real
# GEMINI_API_KEY (see config.py). Lightweight, no GPU/torch needed.
google-genai
groq

# --- /chat command (Akbots/chatbot.py) — general conversational AI.
# Previously ran a local HuggingFace "conversational" pipeline
# (facebook/blenderbot-400M-distill), which needed a multi-GB transformers
# + torch install AND broke outright on any modern transformers version
# (the "conversational" pipeline task was removed upstream). Now reuses
# the same Groq API the /groq command already talks to — no extra
# dependency here at all, `groq` above already covers it.

# --- pydantic ---
# Not previously declared explicitly, left to whatever the lowest common
# denominator of google-genai / fastapi / openai pulled in. Pinned
# explicitly here to a range every one of those actually supports, so pip
# doesn't have to search hundreds of google-genai releases to find one
# that fits.
pydantic>=2.0.0,<3.0.0

# Akbots/nova_bypasser/ (ported from the Nova link-bypasser bots) needs a
# pure-Python JS interpreter for its "execute inline JS redirect" method.
# No browser/Node.js required. selenium is intentionally NOT added here —
# the browser-automation fallback in advanced.py auto-disables itself
# when selenium isn't installed, see the try/except at its top.
js2py

# --- aniworld_lib/ (vendored AniWorld-Downloader, wired via Akbots/aniworld_dl.py) ---
# Full upstream package (aniworld.to / s.to / Kinox / Cineby / BurningSeries /
# MegaKino / FilmPalast / MangaFire / HanimeTV) lives at aniworld_lib/ — a
# sibling of Akbots/, NOT inside it, so Pyrogram's `plugins=dict(root="Akbots")`
# loader never tries to import its ~150 nested modules as bot plugins.
# Only the subset its core resolve chain (config.py -> models -> extractors)
# hard-imports at module load time is added here:
niquests            # aniworld_lib/config.py's HTTP session (DoH resolver)
certifi              # aniworld_lib/config.py's CA bundle
packaging            # aniworld_lib/config.py's version check
cryptography         # aniworld_lib extractors/provider/{filemoon,hanime_tv}.py + models/common/hls.py (AES key handling)
rich                 # aniworld_lib models/s_to/series.py + extractors/provider/hanime_tv.py
# curl_cffi and python-dotenv are already declared above (yt-dlp / other
# plugins already need them) and aniworld_lib reuses those same installs.
#
# patchright — a stealth-patched playwright fork — is a HARD requirement
# for several of aniworld_lib's providers (VOE — the primary AniWorld
# extractor — SerienStream/s.to, Cineby, BurningSeries, HanimeTV all drive
# a real headless Chromium). This repo already installs plain
# `playwright==1.54.0` + Chromium in the Dockerfile for headless.py /
# playwright_bypass.py, but patchright is a separate pip package with its
# own browser cache — see the Dockerfile's added
# `RUN patchright install --with-deps chromium` step.
patchright

# --- freeflix_lib/ (vendored freeflix-cli's scraping/, wired via
# Akbots/freeflix_dl.py: Anime-Sama, Coflix, French-Stream, GoldenAnime,
# GoldenMS). Sibling of Akbots/, same reasoning as aniworld_lib above.
platformdirs   # freeflix_lib/tracker.py's per-OS config dir

# --- anime_downloader/ (vendored anime-downloader-master, wired via
# Akbots/animedl_dl.py). Kept its own top-level package name (upstream uses
# absolute `from anime_downloader.xxx import yyy` throughout — renaming the
# vendored folder would break every one of those imports). cli.py, gui.py,
# watch.py, commands/ and downloader/ (pySmartDL) were stripped out during
# vendoring — CLI/GUI/mpv-playback code with no headless equivalent, plus
# we download via Akbots/meow_downloader.py instead of pySmartDL, same as
# every other *_dl.py plugin in this repo.
#
# NOTE: this package is from 2019-2021 and many of its ~15 still-enabled
# sites (see anime_downloader/sites/init.py's ALL_ANIME_SITES — the
# maintainers already commented out the ones they'd confirmed dead) may no
# longer work in 2026; each site scrapes a specific page structure that
# can drift or the site can vanish entirely with no warning. Wired as-is,
# not re-verified site-by-site here.
click
coloredlogs
tabulate
requests-cache
fuzzywuzzy
python-Levenshtein  # optional but silences fuzzywuzzy's "using slow pure-Python SequenceMatcher" warning

# Crunchyroll downloader plugin (Akbots/crunchyroll.py)
tls_client
tqdm
# pywidevine — Widevine CDM client used by CrunchyrollLicense.get_license()
# (Akbots/crunchyroll_dl/crunchyroll.py) to request/decrypt the content
# license, AND by Akbots/hotstar_widevine.py's extract_key_sync() for
# Hotstar's DASH/Widevine content (services/hotstar-api/main.py's
# run_dash_download — optionally fed a browser-captured license_url from
# Akbots/hotstar_browser.py instead of regex-scanning the MPD for one).
# Requires your own L3 device file at ./l3.wvd (HOTSTAR_WVD_FILE in
# config.py for the Hotstar path) — not something pip installs or this
# repo can provide; it's device-specific credential material you have to
# supply yourself.
pywidevine

# Akbots/anime_hindi_extractors.py + anime_hindi_scraper.py (Toonstream/
# AnimeDekho/HindiSubAnime/HindiAnimeVerse/WatchAnimeWorld/ToonsHub) —
# ported from the Anime-Episode-Scraper project. beautifulsoup4/cloudscraper
# already present above; these two are additional:
PyExecJS   # JS deobfuscation used by a few embed-host extractors (execjs)
numpy      # only used for a minor range-generation helper; safe if missing
