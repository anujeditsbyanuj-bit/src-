# --- Telegram & Bot Frameworks ---
kurigram
TgCrypto

# --- Asynchronous & Networking ---
aiohttp==3.9.5
requests
m3u8                # M3U8 playlist parsing for Akbots/m3u8dl.py

# --- Database & Utilities ---
motor

# --- Web Framework (Keep Alive) ---
Flask==2.3.3
gunicorn==21.2.0
Jinja2==3.1.2
aiofiles           # async template reads for Akbots/filetolink (stream/download watch pages)
werkzeug==2.3.7
itsdangerous==2.1.2

# --- Media Downloader (/yt, /yta) ---
# Pinned to a recent release — YouTube/Instagram break old yt-dlp versions
# often, and an unpinned "yt-dlp" can silently stay on a stale cached
# install across redeploys on Render/Railway. Bump this periodically, or
# run `pip install -U yt-dlp` on the host directly if downloads start
# failing again.
# [default] extra bundles yt-dlp-ejs — the external-JS-challenge-solver
# component yt-dlp has required for full YouTube support since 2025.11.12.
# Without it, extraction can still succeed but the actual googlevideo
# download 403s (signature/n-param never gets solved) — see Akbots/ytdl.py's
# js_runtimes opt for the runtime side of this same requirement.
yt-dlp[default]>=2026.07.04
# NOTE: Akbots/ytdl.py also falls back to a vendored copy of youtube-dl
# (repo root: ytdl_legacy/) as a last-resort extractor tier for links every
# yt-dlp attempt fails on. It's pure stdlib — no pip package needed, nothing
# to add here — just don't delete the ytdl_legacy/ folder from the repo.

# --- YouTube PO Token auto-generation (fixes "Sign in to confirm you're
# not a bot" / 403 errors without needing manual cookies) ---
# Registers itself as a yt-dlp plugin automatically on install; no extra
# code wiring needed. Falls back silently to cookies if unavailable.
bgutil-ytdlp-pot-provider>=1.2.0

# --- Web scraping (Akbots/mediafire.py precise HTML parsing) ---
beautifulsoup4
curl_cffi>=0.10,<0.14  # NOT 0.6.x/0.7.x/0.8.x/0.9.x — yt-dlp's own impersonate feature explicitly rejects those as "unavailable" even though pip installs them fine (see yt-dlp's curl-cffi dependency spec)

# --- Fembed-family embed-host mirrors (Akbots/fembed.py) ---
lk21==1.6.0

# --- Gallery Downloader (/gallery) ---
gallery-dl

# --- Headless-browser video discovery fallback (JS-rendered players yt-dlp
# can't parse from raw HTML) — used by Akbots/headless.py ---
# `pip install playwright` alone does NOT ship the browser binary — it
# needs a separate `playwright install chromium` step. That's now handled
# automatically:
#   - Docker deploys: the Dockerfile runs it at build time.
#   - Non-Docker (Procfile/buildpack) deploys: Akbots/headless.py
#     self-installs it on first use (one-time ~30-90s delay, cached after).
# Either way, no manual step needed. If it can't install for some reason,
# this fallback just no-ops and the bot falls through to gallery-dl /
# raw-file download as before.
# Pinned to 1.54.0 — camoufox (below) needs this exact version; newer
# Node drivers (1.55+) crash on `pageError.location.url` for real
# Cloudflare-protected pages, which is what camoufox is used against.
playwright==1.54.0

# --- Speed Test (/speedtest) ---
speedtest-cli

# --- AutoPost daily scheduler (/autotest, TMDB movie posts) ---
apscheduler

# --- System binaries needed for some downloaders (install via apt, NOT pip) ---
# ffmpeg      -> already required for /fb thumbnails, video metadata
# aria2       -> needed for magnet/.torrent links (/torrent), FTP links
#                (/ftp, Akbots/aria2_dl.py), and resumable direct-HTTP
#                downloads (Akbots/urluploader.py)
# megatools   -> needed for Mega.nz links (/mega)
feedparser

# --- Google Drive folder/private-file support (/gdrive) — OAuth API ---
# Needs an admin-generated token.pickle (see GDRIVE_SETUP.md /
# gdrive_oauth_setup.py) uploaded via /setgdrivetoken. Without it, /gdrive
# just falls back to the existing public-file-only method — nothing breaks.
google-api-python-client
google-auth
google-auth-oauthlib
natsort

# --- Archive tools (/zip, /unzip — Akbots/archive.py) ---
# pyzipper: password-protected zip creation for /zip (AES-256, AES-128, and
# legacy ZipCrypto via /zipencryption). Falls back to a plain (unencrypted)
# zip if this isn't installed — nothing breaks.
# py7zr: pure-Python 7z archive creation for /zipformat 7z — no external
# `7z`/`p7zip` binary needed to build .7z files (unlike /unzip's extraction
# path below, or RAR creation, both of which do need a system binary).
# Falls back to reporting the format unavailable if not installed.
# Extraction (/unzip) uses the system `7z` binary instead (see Dockerfile),
# not a pip package, since it needs to handle rar/7z/tar/gz/etc too.
pyzipper>=0.3.6
py7zr>=0.21.0

# --- JioSaavn music downloader (/saavn — Akbots/jiosaavn.py) ---
# mutagen: writes title/album/artist/cover-art tags onto the downloaded
# .m4a before it's sent.
mutagen

# --- JDownloader integration (/jd — covers hundreds of file hosts yt-dlp
# doesn't: premium hosters, click'n'load containers, DLC files, etc.) ---
# httpx: async HTTP client the myjd/ package (MyJDownloader API client)
# needs to talk to the JDownloader instance. The actual JDownloader.jar +
# Java are installed separately (see Dockerfile / JDOWNLOADER_SETUP.md) —
# this repo only needs httpx for the API side.
httpx

# h2: HTTP/2 support for httpx.AsyncClient(http2=True) — used by
# terabridge_account.py's per-account sessions and terabox.py's shared
# retry client (TeraBridge feature-parity layer: non-blocking client with
# HTTP/2 multiplexing + retry on 5xx).
h2

# --- Headless-browser shortlink bypass (Akbots/playwright_bypass.py) ---
# Fallback bypass tier for shortlink sites the fast HTTP-only dispatcher
# (shortener_lib/) doesn't cover — runs the bundled bypass-shortlinks
# userscript in real Chromium. Reuses the SAME `playwright==1.54.0` pip
# package and Chromium install already set up above for Akbots/headless.py
# (see that block's comments — Dockerfile installs it at build time;
# non-Docker hosts self-install on first use, shared across both modules).
# Nothing extra to add here. If Chromium genuinely isn't available on a
# given host, this tier just fails soft — /bypass and group auto-detect
# keep working via the fast tier's own domain coverage regardless.

# upstash-redis: optional, used only by Akbots/terabridge_account.py for
# persisted multi-account TeraBox rotation (see config.py's
# UPSTASH_REDIS_REST_URL/TOKEN). If those env vars aren't set, that
# module falls back to in-memory single-account mode automatically —
# this package just needs to be importable.
upstash-redis

# --- Cloudflare bypass (Akbots/cf_bypass.py, Akbots/cf_lib/) — two
# in-process paths for scraping sites behind Cloudflare's "Just a
# moment..." / Turnstile challenge, tried in order:
#   1. DrissionPage — a real Chromium browser driven via CDP. Lightweight,
#      no extra service.
#   2. camoufox (below) — a stealth Firefox build, ported in-process from
#      the standalone TurnstileSolver/ project's app/solver.py (now at
#      Akbots/cf_lib/turnstile_solver.py). Stealthier against sites that
#      fingerprint DrissionPage/WebDriver, used as the fallback when #1
#      fails. Needs a running Xvfb (see entrypoint.sh) since real
#      Cloudflare deployments refuse to mount the Turnstile widget for a
#      headless browser.
DrissionPage==4.0.5.6
camoufox[geoip]>=0.4

# --- Shortlink / mirror-host bypass (LinkBypassTG/ and Link-Bypass/ —
# bundled sub-projects for bypassing URL shorteners like exe.io/sub2unlock
# and mirror hosts like GDToT/HubDrive/DriveFire/KatDrive) ---
# cfscrape is deprecated upstream in favor of cloudscraper, but both repos
# still import it directly, so both are kept. NOTE: their own
# requirements.txt also pins urllib3==1.26 — deliberately NOT applied here,
# since it would downgrade the newer urllib3 this project's other
# dependencies (requests/yt-dlp/etc.) expect; pin it yourself in a venv if
# you run either of these two as their own standalone service instead.
cloudscraper
cfscrape
lxml
python-dotenv

# --- /hdhub command + auto-post (Akbots/hdhub.py, Akbots/hdhub_lib/) —
# scraping/caching/storage code ported in from a standalone HdhubScraper
# project as proper submodules of this bot; aiohttp, beautifulsoup4, and
# apscheduler above already satisfy everything it needs. That source
# project's own bot.py (python-telegram-bot based — a second, unrelated
# Telegram framework) and requirements.txt were NOT kept; nothing here
# runs them, only the framework-independent scraper/cache/database logic
# was worth bringing in.

# --- /dub + /dubpdf commands (Akbots/dub.py) — ported in-process from the
# standalone VideoDubbing/ project's pipeline: translate (deep-translator)
# + re-voice (edge-tts) + ffmpeg mux for video, OCR (pymupdf/pdf2image/
# pytesseract) + rebuild for PDFs. VideoDubbing/'s own bot.py, config.py,
# and separate Client/bot token weren't kept — nothing here runs a second
# bot process any more, only its actual pipeline logic, wired into this
# bot's own commands. One deliberate swap vs. the original: transcribes
# with faster-whisper instead of openai-whisper — CTranslate2, not
# PyTorch, so tens of MB instead of multiple GB, and no GPU needed for the
# small/base models this uses on CPU.
ffmpeg-python==0.2.0
edge-tts
deep-translator==1.11.4
indic-transliteration==2.3.82
pymupdf==1.24.1
pdf2image==1.17.0
pytesseract==0.3.10
faster-whisper

# --- Voice cloning for /dub (male1/male2/female — see Akbots/voices/) ---
# Coqui TTS's XTTS-v2 model, used for zero-shot voice cloning from the
# bundled reference clips. Unlike everything else /dub uses, this DOES
# need PyTorch (there's no good lightweight alternative for real cloning
# today) — pull it in only if you actually want cloned-voice dubbing;
# /dub still works with a stock edge-tts voice if this isn't installed.
TTS

# NOT added, on purpose:
#   pyrogram==2.0.106   -> conflicts with this project's `kurigram` (a
#                          Pyrogram fork that installs under the SAME
#                          `pyrogram` import name); installing both would
#                          make pip fight over which one's files "win".
#   openai/whisper (git) -> multi-GB install (pulls in PyTorch), plus
#                          needs a system ffmpeg binary; faster-whisper
#                          (above) covers /dub's transcription instead —
#                          not needed at all now that VideoDubbing/'s own
#                          bot.py isn't kept as a separate process.
